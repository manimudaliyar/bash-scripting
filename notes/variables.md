# Variables

A variable is used to store a value to use later.

Example: userName="Mani"

Note that when creating a variable, there is no space between = and the value

## Using a variable

To use a variable, we have to call it using '$'

Example: $userName
